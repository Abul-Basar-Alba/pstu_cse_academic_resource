# Quick Start Guide - Bangla
# দ্রুত শুরু করার নির্দেশনা

## প্রজেক্ট চালানোর ধাপসমূহ

### ১. প্রয়োজনীয় সফটওয়্যার ইনস্টল করুন

```bash
# Python 3 ইনস্টল করা আছে কিনা চেক করুন
python3 --version

# Flask ইনস্টল করুন
pip install flask
```

### ২. প্রজেক্ট ফোল্ডারে যান

```bash
cd "/mnt/AE587D7D587D44DD/5Th_Semester/CCE-314(Networking Sessional)/Socket_Programing_Project"
```

### ৩. স্ক্রিপ্টগুলিকে এক্সিকিউটেবল করুন

```bash
chmod +x scripts/run_all.sh
chmod +x scripts/stop_all.sh
```

### ৪. সব ইলেক্টরেট চালু করুন

```bash
./scripts/run_all.sh
```

অথবা আলাদা আলাদাভাবে:

```bash
# টার্মিনাল ১ এ
python3 app.py 1

# টার্মিনাল ২ এ
python3 app.py 2

# টার্মিনাল ৩ এ
python3 app.py 3

# টার্মিনাল ৪ এ
python3 app.py 4

# টার্মিনাল ৫ এ
python3 app.py 5
```

### ৫. ব্রাউজারে খুলুন

এই URLগুলি ৫টি আলাদা ট্যাবে খুলুন:

- http://localhost:5001 (ইলেক্টরেট ১)
- http://localhost:5002 (ইলেক্টরেট ২)
- http://localhost:5003 (ইলেক্টরেট ৩)
- http://localhost:5004 (ইলেক্টরেট ৪)
- http://localhost:5005 (ইলেক্টরেট ৫)

### ৬. ভোট দিন

প্রতিটি ট্যাবে গিয়ে Candidate A অথবা Candidate B এর জন্য ভোট দিন।

### ৭. ফলাফল দেখুন

সব ভোট দেওয়ার পরে স্বয়ংক্রিয়ভাবে রেজাল্ট দেখাবে এবং বিজয়ী ঘোষণা করবে!

---

## প্রেজেন্টেশনের জন্য

### স্লাইড তৈরি করুন:
- `presentation/slides_content.md` ফাইলটি দেখুন
- PowerPoint বা Google Slides এ স্লাইড বানান
- কোড স্নিপেট এবং ডায়াগ্রাম যোগ করুন

### ভিডিও রেকর্ড করুন:
1. OBS Studio বা Zoom খুলুন
2. ওয়েবক্যাম চালু করুন (picture-in-picture)
3. স্লাইড শেয়ার করুন
4. লাইভ ডেমো দেখান
5. ১০ মিনিটের মধ্যে রাখুন

### ডেমোতে যা দেখাতে হবে:
- ✅ সকেট প্রোগ্রামিং কনসেপ্ট
- ✅ পাইথন কোড ব্যাখ্যা
- ✅ ওয়েব ইন্টারফেস
- ✅ লাইভ ভোটিং প্রসেস
- ✅ রেজাল্ট ক্যালকুলেশন

---

## সমস্যা সমাধান

### সমস্যা: Port already in use
```bash
# প্রসেস বন্ধ করুন
./scripts/stop_all.sh
# অথবা ম্যানুয়ালি
pkill -f "python.*app.py"
```

### সমস্যা: Flask not found
```bash
pip install flask
# অথবা
pip3 install flask
```

### সমস্যা: Permission denied
```bash
chmod +x scripts/run_all.sh
chmod +x scripts/stop_all.sh
```

---

## প্রজেক্টের গুরুত্বপূর্ণ ফাইল

```
Socket_Programing_Project/
├── app.py                 # মূল ওয়েব অ্যাপ্লিকেশন
├── electorate.py          # সকেট প্রোগ্রামিং লজিক
├── templates/index.html   # ওয়েব ইন্টারফেস
├── static/
│   ├── style.css         # স্টাইলিং
│   └── script.js         # ফ্রন্টএন্ড লজিক
├── README.md             # বিস্তারিত ডকুমেন্টেশন
├── PRESENTATION_GUIDE.md # প্রেজেন্টেশন গাইড
└── scripts/
    ├── run_all.sh        # সব চালু করার স্ক্রিপ্ট
    └── stop_all.sh       # সব বন্ধ করার স্ক্রিপ্ট
```

---

## প্রেজেন্টেশনে কি কি বলবেন

### ইংরেজিতে:

**Introduction:**
"Hello, I'm [Your Name]. Today I'm presenting an election voting system using socket programming with multicast communication."

**Socket Programming:**
"I used UDP multicast sockets to enable one-to-many communication. All five electorates join the multicast group 224.0.0.1 on port 5007."

**Python Implementation:**
"The system uses Python's socket library for networking, threading for concurrent operations, and Flask for the web interface."

**Demo:**
"Let me demonstrate how the system works. I have 5 electorates running. When one casts a vote, all others receive it via multicast."

**Conclusion:**
"This project demonstrates distributed voting using multicast sockets, where each electorate independently determines the winner."

---

## ভালো নম্বরের জন্য টিপস

1. ✅ কোড ভালোভাবে ব্যাখ্যা করুন
2. ✅ সকেট প্রোগ্রামিং কনসেপ্ট পরিষ্কার করুন
3. ✅ লাইভ ডেমো মসৃণভাবে চালান
4. ✅ নিজের চেহারা ভিডিওতে দেখান
5. ✅ ইংরেজিতে আত্মবিশ্বাসের সাথে উপস্থাপন করুন
6. ✅ প্রশ্নের উত্তর দেওয়ার জন্য প্রস্তুত থাকুন

---

**শুভকামনা! আপনার প্রজেক্ট সফল হোক! 🎉**
